{
  "name": "synthex",
  "private": true,
  "version": "0.0.1",
  "description": "DeFi application (proof of concept) like Synthetix (WIP).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "4.8.3"
  },
  "devDependencies": {
    "truffle-assert": "^0.9.1"
  }
}
